### What this changes

### Why

### The invariant it touches, if any

The README lists what this package guarantees and what enforces each one. If
this change touches one of those rows, say which and how the enforcement
changed.

---

- [ ] `npm test` passes
- [ ] Commits are signed off (`git commit -s`) — see [CONTRIBUTING.md](../CONTRIBUTING.md)
- [ ] New behaviour has a test that asserts the behaviour, not that a file contains a string
